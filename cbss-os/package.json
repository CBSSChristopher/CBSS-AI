{
  "name": "cbssos",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:yard": "wrangler deploy --config wrangler.harbor.jsonc",
    "test": "node --experimental-strip-types --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
