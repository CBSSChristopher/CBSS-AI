{
  "name": "cbssinvoice",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "node --experimental-strip-types --test test/*.test.mjs",
    "render-packet": "node --experimental-strip-types scripts/render-palmer-packet.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
